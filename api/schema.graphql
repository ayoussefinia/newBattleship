  type Query {
    ping: String
  }

  type Mutation {
    takeTurn(playerId: String!, opponentId: String!, data: String): String
  }

  type Subscription {
    playerConnected(game: String!): Game
  }

  type Game {
    playerId: String
    opponentId: String
    data: String
  }
