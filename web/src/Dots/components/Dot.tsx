const dotStyle = {
    background: 'purple',
    borderRadius: '50%',
    height: '20px',
    width: '20px'
};

const Dot = () => {

    return(
        <div>
            <div style={dotStyle}></div>
        </div>
    );
}

export default Dot;

//import React, { Component, ReactElement, useState } from 'react';